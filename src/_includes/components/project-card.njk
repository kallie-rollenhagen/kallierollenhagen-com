{% set side = "left" if loop.index0 % 2 == 0 else "right" %}
<div class='project-card-wrapper half-photo-{{ side }}'>
  <a href="{{ project.url | url }}"
    class="project-card no-link">

    <img src="{{ project.data.thumbnail_image.file | url }}"
        alt="{{ project.data.title }}"
        class="half-photo-{{ side }}"
        style="object-position: {{ project.data.thumbnail_image.focal_x or 50 }}% {{ project.data.thumbnail_image.focal_y or 50 }}%">

    <div class="project-overlay">
      <div class="project-info">
        <h3>{{ project.data.title }}</h3>
        <p>{{ project.data.roles_played }}</p>
      </div>
    </div>
  </a>
</div>